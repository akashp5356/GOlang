package main
 import "fmt"
func main(){
	var name string="ABC"
	fmt.Println(name)
	//Printf formats according to a format specifier and writes to standard output
	fmt.Printf("Variabe is of type: %T",name ) 
}